server:
  port: 8082

spring:
  application:
    name: supplier
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-rapid-cell-apza906x-pooler.c-7.us-east-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_U2M5GuKozFJc&sslmode=require&channelBinding=require}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  jackson:
    default-property-inclusion: non_null

app:
  jwt:
    secret: ${APP_JWT_SECRET:cloud-pos-supplier-marketplace-super-secret-key-change-in-production}
    expiration-ms: ${APP_JWT_EXPIRATION_MS:86400000}

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    org.hibernate.SQL: debug
